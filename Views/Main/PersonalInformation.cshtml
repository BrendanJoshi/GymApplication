@*   @model IEnumerable<PersonalInfromationViewModel>

        <div class="row page-titles mx-0">
            <div class="col-sm-6 p-md-0">
                <div class="welcome-text">
                    <h4>Hi, welcome back!</h4>
                    <span class="ml-1">Personal Information</span>
                </div>
            </div>
            <div class="col-sm-6 p-md-0 justify-content-sm-end mt-2 mt-sm-0 d-flex">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="javascript:void(0)">Setup</a></li>
                    <li class="breadcrumb-item active"><a href="javascript:void(0)">Personal Information</a></li>
                </ol>
            </div>
        </div>
      
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
            <h4 class="card-title">Personal Information</h4>
            <a asp-action="createPersonalInformation" class="btn btn-info btn-sm" style="margin-right: 5px;">
                <i class="fa fa-add"></i> Create
            </a>
                    </div>
                    <div>
       
        </div>
        <div class="card-body">
                        <div class="table-responsive">
                            <table id="example" class="display" style="min-width: 845px">
                 
                    <thead>
                        <tr style="text-align: center;">
                            <th>SN</th>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>Gender</th>
                            <th>Mobile Number</th>
                           
                            <th>Email</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            int i = 1;
                            foreach (var item in Model)
                            {
                                <tr style="text-align: center;">
                                    <td>@i</td>
                                    <td>@item.FirstName</td>
                                    <td>@item.LastName</td>
                                    <td>@item.GenderName</td>
                                    <td>@item.MobileNumber</td>
                                  
                                    <td>@item.Email</td>
                                    <td>
                                        <a asp-action="PersonalInformationDetails" asp-route-id="@item.Id" class="btn btn-info btn-sm" style="margin-right: 5px;">
                                            <i class="fa fa-eye"></i> View
                                        </a>
                                        <a asp-action="CreatePersonalInformation" asp-route-id="@item.Id" class="btn btn-warning btn-sm" style="margin-right: 5px;">
                                            <i class="fa fa-edit"></i> Edit
                                        </a>

                                    </td>
                                </tr>
                                i++;
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="8" style="text-align: center; font-weight: bold;">No Records Found</td>
                            </tr>
                        }

                    </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
 

 *@